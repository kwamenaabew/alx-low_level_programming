#include "main.h"

/**
 * _isupper - main function
 * This program returns 1 if
 * @c: is uppercase or 0 if otherwise
 * Return: 0
 */

int _isupper(int c)
{
	char a;

	for (a = 'A'; a <= 'Z'; a++)
	{
		if (c == a)
		{;
			return (1);
		}
	}
	return (
}
