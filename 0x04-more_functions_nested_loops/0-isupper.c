#include "main.h"

/**
 * _isupper - main function
 * This program returns 1 if
 * @c: is uppercase or 0 if otherwise
 * Return: 0
 */

int _isupper(int c)
{
	char x;

	for (x = 'A'; x <= 'Z'; z++)
	{
		if (c == x)
		{
			return (1);
		}
	}
	return (0);
}
